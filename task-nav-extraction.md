# LinkHaiTao 导航结构提取任务 (无登录/无遍历)

**目标**：仅识别当前页面的导航栏结构并保存到文件，**不需要登录步骤**，也不进行点击遍历。

## ⚠️ 关键操作 (必须遵守)
1. **禁止点击**：不要执行任何页面跳转或点击操作（除非为了展开各级菜单以查看结构）。
2. **状态管理**：主要任务是观察并调用 `save_navigation_structure`。

---

## 🚀 执行步骤

### 参考：直接从当前页面开始
- 假设 Agent 已经打开了目标网页（或用户已手动登录）。不需要执行登录动作。

### 1. 准备工作：检查侧边栏状态
1. **观察**：侧边栏是展开的（能看到文字）还是折叠的（只看到图标）？
2. **行动**：
   - 如果是**折叠状态**：查找并点击 "Toggle", "Collapse", "Menu" 或 ☰ 等图标按钮，直到侧边栏展开并显示文字。
   - 如果已展开：跳过此步。

### 2. 获取并保存结构
1. **调用工具**识别导航栏结构
2. **保存结果**：
   - 立即调用 `save_navigation_structure(json_result)`。
3. **记住**工具返回的文件名。

### 3. 输出与结束
1. 调用 `AgentOutput` 输出："导航结构已提取并保存为 [文件名]。"
2. 任务结束。